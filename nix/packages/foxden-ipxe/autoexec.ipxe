#!ipxe

isset ${next-server} || chain --replace --autofree file:/autoexec.ipxe || shell || exit

set tftp_url tftp://${next-server}:69

prompt --key 0x02 --timeout 2000 Press Ctrl-B for the iPXE command line... && shell ||

ifopen
chain --replace --autofree ${tftp_url}/autoexec.ipxe
