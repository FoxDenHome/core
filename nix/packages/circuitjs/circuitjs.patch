diff --git a/build.gradle b/build.gradle
index 68c34bb..59d99c2 100755
--- a/build.gradle
+++ b/build.gradle
@@ -21,9 +21,6 @@
 
 // This must be before plugins!
 buildscript {
-    repositories {
-      mavenCentral()
-    }
     dependencies {
         classpath("org.wisepersist:gwt-gradle-plugin:1.1.19")
     }
@@ -36,20 +33,6 @@ plugins {
     //id 'war'                                      // 
 }
 
-// https://docs.gradle.org/current/userguide/repository_types.html
-repositories {
-    //flatDir { dirs "lib" }        // use a (flat) filesystem directory as a repository
-    mavenLocal()                    // use the your local Maven cache as a repository
-    mavenCentral()                  // JUnit 5 and other OSS artifacts
-    //mavenCentral()                // use MC for popular OSS Java libraries
-    //jcenter()                     // use Bintray's JCenter for Maven OSS artifacts
-    //google()                      // use Android-specific artifacts including the Android SDK
-    maven {
-        url = 'https://plugins.gradle.org/m2/'
-        url = 'https://repo.maven.apache.org/maven2'
-    }
-}
-
 dependencies {
     implementation 'com.google.gwt:gwt-user:2.9.0'
     implementation 'com.google.gwt:gwt-dev:2.9.0'
diff --git a/src/com/lushprojects/circuitjs1/client/CirSim.java b/src/com/lushprojects/circuitjs1/client/CirSim.java
index 0addecf..4209b17 100644
--- a/src/com/lushprojects/circuitjs1/client/CirSim.java
+++ b/src/com/lushprojects/circuitjs1/client/CirSim.java
@@ -170,9 +170,9 @@ MouseOutHandler, MouseWheelHandler {
     CheckboxMenuItem weightedPriorityCheckItem;
     
     enum ToolbarType { ELECTRONICS, ECONOMICS }
-    ToolbarType currentToolbarType = ToolbarType.ECONOMICS;
-    String voltageUnitSymbol = "$"; // Custom voltage unit symbol ($ for economics default)
-    String timeUnitSymbol = "yr"; // Custom time unit symbol (yr for economics default)
+    ToolbarType currentToolbarType = ToolbarType.ELECTRONICS;
+    String voltageUnitSymbol = "V"; // Custom voltage unit symbol (V for electronics default)
+    String timeUnitSymbol = "s"; // Custom time unit symbol (s for electronics default)
 	int infoViewerUpdateIntervalMs = 100; // InfoViewer live update throttling interval
     boolean useWeightedPriority = false; // Weighted priority for Asset/Equity columns
     String modelInfoContent = null; // Markdown info content from @info block in SFCR files
@@ -957,7 +957,7 @@ public CirSim() {
 	    noEditing = !qp.getBooleanValue("editable", true);
 	    mouseWheelEdit = qp.getBooleanValue("mouseWheelEdit", getOptionFromStorage("mouseWheelEdit", true));
 	    useWeightedPriority = getOptionFromStorage("weightedPriority", false);
-	    showElectronicsCircuits = getOptionFromStorage("showElectronicsCircuits", false);
+	    showElectronicsCircuits = getOptionFromStorage("showElectronicsCircuits", true);
 	    enableCacheBustedUrls = getOptionFromStorage("enableCacheBustedUrls", true);
 	    tableRenderCacheEnabled = getOptionFromStorage("tableRenderCacheEnabled", true);
 	    equationTableNewtonJacobianEnabled = getOptionFromStorage("equationTableNewtonJacobianEnabled", false);
@@ -1249,7 +1249,7 @@ public CirSim() {
 	DOM.appendChild(layoutPanel.getElement(), topPanelCheckbox);
 	DOM.appendChild(layoutPanel.getElement(), topPanelCheckboxLabel);	
 
-	toolbar = new EconomicsToolbar();
+	toolbar = new ElectronicsToolbar();
 	toolbar.setEuroResistors(euroSetting);
 	if (!hideMenu)
 	    layoutPanel.addNorth(menuBar, MENUBARHEIGHT);
diff --git a/war/circuitjs.html b/war/circuitjs.html
index c763160..63c5337 100644
--- a/war/circuitjs.html
+++ b/war/circuitjs.html
@@ -1,14 +1,14 @@
 <!DOCTYPE html>
 <html>
   <head>
-  <link rel="manifest" href="/circuit/manifest.json">
+  <link rel="manifest" href="/manifest.json">
   
     <!-- Properties can be specified to influence deferred binding -->
     <meta name='gwt:property' content='locale=en_UK'>
     
     
     <!-- Titles are optional, but useful -->
-    <title></title>
+    <title>FoxDen CircuitJS</title>
 
 <style>
 .disabled {
@@ -241,11 +241,7 @@ input.locked-column[readonly] {
   <!-- URL shortener relay configuration -->
   <!-- Uses local PHP on localhost, Cloudflare Worker on production -->
   <script>
-    if (window.location.hostname === 'localhost' || window.location.hostname === '127.0.0.1') {
-      window.shortRelayUrl = "shortrelay.php";
-    } else {
-      window.shortRelayUrl = "https://circuitjs-shortener.johnnewto.workers.dev";
-    }
+	window.shortRelayUrl = "/shorturl";
   </script>
 
   <script language="javascript" src="lz-string.min.js"></script>
diff --git a/war/manifest.json b/war/manifest.json
index f8a79dc..793107e 100644
--- a/war/manifest.json
+++ b/war/manifest.json
@@ -1,19 +1,19 @@
 {
   "name": "CircuitJS1",
   "short_name": "CircuitJS1",
-  "start_url": "/circuit/circuitjs.html",
+  "start_url": "/circuitjs.html",
   "display": "standalone",
   "background_color": "#000000",
   "theme_color": "#2196f3",
   "description": "Circuit Simulator",
   "icons": [
     {
-      "src": "/circuit/icon512.png",
+      "src": "/icon512.png",
       "sizes": "512x512",
       "type": "image/png"
     },
     {
-      "src": "/circuit/icon1024.png",
+      "src": "/icon1024.png",
       "sizes": "128x128",
       "type": "image/png"
     }
diff --git a/war/service-worker.js b/war/service-worker.js
index b7af0a8..fd473a2 100644
--- a/war/service-worker.js
+++ b/war/service-worker.js
@@ -1,23 +1,23 @@
 const CACHE_NAME = 'circuitjs1-app-cache-v1';
 const urlsToCache = [
-  '/circuit/about.html',
-  '/circuit/canvas2svg.js',
-  '/circuit/circuitjs.html',
-  '/circuit/crystal.html',
-  '/circuit/customfunction.html',
-  '/circuit/customlogic.html',
-  '/circuit/customtransformer.html',
-  '/circuit/diodecalc.html',
-  '/circuit/icon512.png',
-  '/circuit/icon128.png',
-  '/circuit/iframe.html',
-  '/circuit/lz-string.min.js',
-  '/circuit/manifest.json',
-  '/circuit/mexle.html',
-  '/circuit/mosfet-beta.html',
-  '/circuit/opampreal.html',
-  '/circuit/split.js',
-  '/circuit/subcircuits.html',
+  '/about.html',
+  '/canvas2svg.js',
+  '/circuitjs.html',
+  '/crystal.html',
+  '/customfunction.html',
+  '/customlogic.html',
+  '/customtransformer.html',
+  '/diodecalc.html',
+  '/icon512.png',
+  '/icon128.png',
+  '/iframe.html',
+  '/lz-string.min.js',
+  '/manifest.json',
+  '/mexle.html',
+  '/mosfet-beta.html',
+  '/opampreal.html',
+  '/split.js',
+  '/subcircuits.html',
   // put everything else here
 ];
 
