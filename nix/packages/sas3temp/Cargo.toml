[package]
name = "sas3temp"
version = "0.1.0"
edition = "2021"
rust-version = "1.77"
description = "Read the IOC/ROC temperature of LSI Fusion-MPT HBAs (SAS2/SAS3) without storcli"
license = "MIT OR Apache-2.0"

[dependencies]

[profile.release]
opt-level = "s"
lto = true
strip = true
panic = "abort"
