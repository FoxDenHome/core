on: [push]
jobs:
  lint:
    runs-on: default
    steps:
      - name: checkout code
        uses: actions/checkout@v4
      - name: Say hi
        run: echo Hello
      - name: Build dev container
        run: podman build -t core .
